const express = require('express');
const app        = express();
const router  = express.Router();

module.exports = (db) => {

  app.get('/:id', (req, res) => {

    // set cookie


    // redirect home

  })

}
